# URL rewriting: pretty URL -> index.php?url=...
# Reálné soubory (uploads/) a složky nech projít beze změny.

RewriteEngine On

RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d
RewriteRule ^(.+)$ index.php?url=$1 [QSA,L]
